apiVersion: v1
kind: Service
metadata:
  name: {{ include "defensive-toolkit.fullname" . }}
  labels:
    {{- include "defensive-toolkit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "defensive-toolkit.selectorLabels" . | nindent 4 }}
